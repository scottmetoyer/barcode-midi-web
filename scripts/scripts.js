document.addEventListener("DOMContentLoaded", function(){
  // Add available MIDI interfaces to the select list
  var select = document.getElementById('interfaceSelect');
  // select.options[select.options.length] = new Option('Option 1', '1', false, false);
});